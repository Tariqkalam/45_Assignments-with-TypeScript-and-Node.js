let transportations: string[] = ['Motor Vehicle', 'Motorbike', 'Train', 'Aeroplan'];

for (let i = 0; i< transportations.length; i++) {

    console.log('I Would like to own a '+ transportations[i]);
    
}